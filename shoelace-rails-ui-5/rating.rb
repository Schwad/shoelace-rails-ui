module ShoelaceRailsUI
  def sl_rating(getSymbol: (value: number) => string = (value:, &block)
    content_tag(rating getSymbol: (value: number) => string = (value=#{getSymbol: (value: number) => string = (value}, yield)
  end
end
